# @package _group_
network:
  model: "CXRdecomp4"
  use_norm: "none"
  activation_func: "relu"
  input_views: 1

general:
  batch_size: 1
  learning_rate: 0.00001
  loss: "BCEloss"
  overfit_batches: 0
